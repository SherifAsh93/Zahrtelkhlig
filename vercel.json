{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && node prisma/seed-categories.cjs && npm run build",
  "installCommand": "npm install",
  "devCommand": "npm run dev"
}
